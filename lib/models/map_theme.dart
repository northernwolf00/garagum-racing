/// Identifies which playable map/art-set a round belongs to. Threaded
/// through round config, terrain, obstacles, parallax, decor, the car and
/// progress storage so each map can be reskinned independently.
enum MapTheme { garagum, ashgabat }
